services:
  frontend:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: targetlayer-frontend:latest
    container_name: targetlayer-frontend
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "${FRONTEND_HTTP_PORT:-8080}:80"
